{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci --prefer-offline && npx prisma generate && npx tsc --skipLibCheck --force"
  },
  "deploy": {
    "startCommand": "node dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
