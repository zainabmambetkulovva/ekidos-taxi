{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "node_modules/.bin/prisma generate && node_modules/.bin/tsc --skipLibCheck"
  },
  "deploy": {
    "startCommand": "node dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
